import {
    FORMULARIO_CATEGORIAS,
    OBTENER_CATEGORIAS,
    AGREGAR_CATEGORIA,
    VALIDAR_FORMULARIO,
    CATEGORIA_ACTUAL,
    ELIMINAR_CATEGORIA,
    HISTORIAS_CATEGORIAS,
    CATEGORIA_ERROR
} from '../../types';

export default (state, action) => {
    switch (action.type) {        
        default:
            return state;
    }
}